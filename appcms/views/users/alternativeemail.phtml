{% extends "template/layout/appcmscpaneltemplate.phtml" %}
{% block cpanelcontent  %}
<div id="cPanPages">
    <h1>{{ pageTitle }}</h1>
    <div class="breadcrumb-font"><a href="{{ indexpage }}"><< {{ breadcrumb_index }} Home</a></div>
    <hr>
    <div class="row">
        <form action="{{ processForm }}" method="post" class="ajax" name="{{ formName }}" >
            <div class="cpan-input-wrapper">
                <h3>ALTERNATIVE EMAIL</h3>
                <div class="form-field-para">Your alternative email address is:  {{ alterEmail }}</div>
                <label>Add Alternative Email Address: </label>
                <input type="text" name="user-alternative-email" id="user-alternative-email" class="input-form-fields" value=" ">
            </div>
            <hr>
            <input type="submit" name="{{ submitButton }}" value="Update Email Info" class="cpan-forms-submit-button" />
        </form>
    </div>
</div>
{% endblock %}